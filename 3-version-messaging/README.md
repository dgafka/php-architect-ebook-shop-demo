# PHP Architect Demo Application

## Configuration

Run `composer install`

## Run the application

1. Run database migrations `vendor/bin/doctrine-migrations migrations:migrate --no-interaction` 
2. Run the example `run_example.php`